#include <iostream>
using namespace std;

#define fastIO ios_base::sync_with_stdio(0);cin.tie(0);

char ar[101][51]

int main() {
	fastIO;
	int r, c; cin >> c >> r;
	for(int i = 0; i < r; i++) {
		for(int j = 0; j < c; j++) {
			cin >> ar[r][c];
		}
	}
	for(int i = 0; i < r; i++) {
		int sum = 0;
		for(int j = 0; j < c-1; j++) {
			sum += abs(char[j] - char[j-1]);
		}
		sns[i][] = sns
	}
	return 0;
}